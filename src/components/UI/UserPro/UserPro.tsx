import React, { FC } from "react";

const UserPro: FC = () => {
  return (
    <span
      style={{
        background: "#ffc107",
        color: "#000",
        fontWeight: 600,
        padding: "1px",
      }}
    >
      PRO
    </span>
  );
};

export default UserPro;
